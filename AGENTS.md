# 项目与发布边界

本目录是「加班记录」，唯一正式 Pages 项目为 `jiaban`，网址为 `https://jiaban-x2m.pages.dev/`。

- 实际发布只能使用本目录的 `deploy.bat` 或 `cf_deploy.py`，保留脚本中的应用身份、域名、文件清单及同步接口校验。不要使用临时上传脚本或另一项目的配置绕过校验。
- 「生活记账」属于 `F:/py/counts`，其项目为 `jizhang`，网址为 `https://jizhang-d9k.pages.dev/`。不得将两个项目的构建产物、发布目标、数据库或 APK 相互替换。
- 日常发布不初始化、重绑或清空数据库。`cloud_setup.py --apply` 仅用于明确需要的首次初始化或配置修复。
- 发布后必须核对正式首页与本地发布文件一致，并确认 `/api/health` 返回 JSON 就绪状态。API 提交成功不等于站点验收成功。
- 保留原签名、应用 ID、同步数据库和用户记录；修复缓存时不要清空 localStorage。仅在隔离环境和临时空间测试。
- 不在输出、公开资源或分享文件中暴露 `.cf_env`、签名文件或用户同步码。
